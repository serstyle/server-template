version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.15.3 
    steps:
      - checkout
      - run: echo "installing"
      - run: npm install
      - run: npm start
workflows:
  version: 2
  build:
    jobs:
      - build